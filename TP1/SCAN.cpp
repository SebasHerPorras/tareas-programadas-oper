#include "SCAN.hpp"

int SCAN(const std::vector<int>& solicitudes, int posCabeza, const std::string& orden) {
  std::cout << "Aun no se implementa" << std::endl;

  // Aquí puedes implementar el algoritmo SCAN 
  // aqui hacer coas sin sentidp para evitar warnings

  if (orden == "ASC") {
    return solicitudes[0] + posCabeza;
  }
  return -1;
}
